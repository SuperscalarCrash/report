#import "template/report.typ": *

#show: project.with(
  theme: "project",
  course: [NSCSCC 2026 决赛设计报告],
  title: [Gemmont：面向 AI Agent 的 \ 高性能乱序超标量 LA32R 处理器],
  header-title: "Gemmont 决赛设计报告",
  language: "zh",
  author: "朱柯宇 刘烨 易好 黄钰",
  date: datetime.today().display("[year] 年 [month padding:none] 月 [day padding:none] 日"),
  cover_cells: (
    ([参赛学校], [浙江大学]),
    ([队　　长], [朱柯宇]),
    ([队　　员], [刘　烨]),
    ([队　　员], [易　好]),
    ([队　　员], [黄　钰]),
    ([指导教师], [常瑞　申文博]),
    ([提交日期], [2026 年 8 月 19 日]),
  ),
)

#set heading(supplement: none)

= 项目介绍

*Gemmont*#footnote[
  「Gemmont」一词由「Gem」（宝石）与「-mont」缀合而成。其一，宝石山为杭州名胜，与本队所在学校同城；其二，致敬 Intel 以「-mont」后缀命名其低功耗核心的传统；其三，拼写上前四字母恰构成 GEMM（General Matrix Multiplication，通用矩阵乘），以这一最重要的计算内核之一寄寓对处理器性能的追求。] 是第十届「龙芯杯」全国大学生计算机系统能力大赛 CPU 设计赛 (NSCSCC 2026) 的参赛作品。

Gemmont 是一款 32 位龙架构精简版（LoongArch32 Reduced / LA32R）*乱序超标量*处理器核心，采用基于 Tomasulo 算法与重排序缓存（ROB）的乱序多发射微架构，包括 5 条执行流水线，4 取指、3 译码、3 提交，最长 13 级流水线。相较往届作品，我们在微架构上的主要创新为引入了 *AI 分支预测器*、64 KiB 统一 *L2 缓存*、*L1 预取器*等。

性能方面，我们最终主频达到 _109.09 MHz_，在所有乱序核中排名第一；周期数相对 openLA500 基线几何平均加速 _1.975x_，总加速 _6.583x_；最终成绩在所有参赛队伍中_排名 4/549_。#footnote[由于 CI 综合时长限制、环境差异、CI 时间紧张等因素，正式提交大赛 CI 的记录成绩为 107.69 MHz 主频与 1.898 周期加速比。]

系统展示方面，为替换大赛提供的 Linux 5.14.0-rc2 陈旧内核与 GCC 8 等工具链，我们*将最新 Linux 7.1.4 主线内核、最新 GCC 16、glibc 2.44 与 Go 语言移植至 LA32R 指令集*。我们使用 Buildroot 构建包含 SSH、X 桌面、PicoClaw 等应用软件的*嵌入式 Linux 发行版*，并通过 NFS root 启动。另外，我们在微架构上设置 DP4 点积扩展指令，在端侧实现了 *10 s/token 的大模型推理*。Gemmont 可以*稳定启动 Linux* 并运行发行版中所有软件，同时驱动板上包括全速 USB 在内的*全部外设*。

= 微架构设计

Gemmont 采用基于 Tomasulo 算法和重排序缓存（ROB）的乱序多发射结构。前端每周期最多取指 4 条、译码和分发 3 条，后端设置 5 条执行流水线并最多提交 3 条指令，最长流水线为 13 级。

Gemmont 的整体架构如 @fig:microarch 所示。

#figure(
  image("figs/arch.drawio.pdf"),
  caption: [Gemmont 核微架构设计图],
) <fig:microarch>

== 功能支持

Gemmont 实现了运行 Linux 所需的 LA32R 指令、控制状态寄存器、精确异常和中断处理，并支持 LL/SC、缓存维护与栅障指令。核内有两组 32 项全相联 TLB，取指和访存分别使用，覆盖直接地址、直接映射窗口和页表映射三种路径。取指、访存和特权操作产生的异常均随指令进入 ROB，直至提交时生效，因此乱序执行不会改变架构可见的异常顺序。

在标准指令之外，Gemmont 实现了用于 INT8 点积的 `DP4` 扩展；`CPUCFG` 与 `PRELOAD` 则按比赛软件的兼容要求处理。处理器已通过功能测试、性能测试和 Linux 启动验证。

核外接口为 32 位 AXI3，并接收 8 位硬件中断。一级指令缓存、一级数据缓存和未缓存通路在核内汇入统一 L2，对外只保留一个 AXI 主口。另外，Difftest 接口独立输出提交指令、寄存器和 CSR 状态。

== 前端

#figure(
  image("figs/frontend.drawio.pdf", width: 100%),
  caption: [Gemmont 前端结构],
) <fig:frontend>

前端由取指 1（IF1）、取指 2（IF2）两级流水与一个 8 项取指缓冲组成，其后衔接译码（DECODE）、重命名（RENAME）、分发（DISPATCH）三级。

=== 取指与译码

*取指 1 阶段。*程序计数器（PC）组合地产生当前要流入流水线的指令地址，该地址不跨越 64 字节缓存行边界，同拍被运用到指令缓存的同步读口与分支预测器的查询口。PC 只在指令缓存请求被接受、预测重定向或后端重定向被应用时才前进；当后端重定向到来而流水线处于阻塞状态时，重定向目标被锁存，待阻塞解除后重放，保证重定向不丢失。取指地址的虚实转换也在本阶段完成。

*取指 2 阶段。*指令缓存的同步读结果与分支预测器的响应按 PC 对齐，组合成至多 4 条指令的取指包，经一级弹性寄存器展开后压入 8 项取指缓冲。当指令缓存缺失导致长时间停顿时，预测器响应被寄存并保持稳定。

*译码阶段。*译码为单级纯组合逻辑，每周期从取指缓冲弹出至多 3 条指令并行译码。译码器实现了 65 条 LA32R 指令与自定义 `DP4` 点积扩展指令的优先级敏感译码表，产出功能单元分类、操作数使用情况、写回信息以及冲刷/串行化标记；译码期同时完成特权等级检查，非特权级执行特权指令将被标记为指令特权等级错例外（IPE）。分支预测信息不经译码器，而与译码产物并列打包，随指令一同进入重命名阶段。

=== 分支预测

分支预测由快速预测路径与修正层组成：快速路径包含分支目标缓存（BTB）、基于全局历史的模式历史表（PHT）与返回地址栈（RAS），修正层为流水化的神经网络分支预测器。

*分支目标缓存（BTB）。*BTB 共 1024 项，采用直接映射，索引为当前指令地址的 `pc[11:2]`，标签为 `pc[31:12]`。每个表项存储跳转目标地址、指令类型（静态目标跳转、调用、返回）与 BTFNT 偏置。取指包的 4 个通道按物理 bank 并行查询同一行组，包内最早的预测跳转分支获得重定向权。

*模式历史表（PHT）。*PHT 共 8192 项，每项为一个 2 位饱和计数器，索引为 `{GHR ^ pc[14:7], pc[6:2]}`——8 位全局历史寄存器（GHR）与指令地址 `pc[14:7]` 异或后作为高 8 位、`pc[6:2]` 作为低 5 位，兼顾全局分支行为与特定位置分支的倾向性；表按四个物理 bank 组织，与取指包四通道一一对应，包内各通道共享同一物理行。BTB 表项保存 BTFNT 偏置，计数器预测「与偏置一致/不一致」。

*返回地址栈（RAS）。*RAS 共 8 项，针对调用与返回指令的后进先出性质进行优化。参考 LA32R 编译器的约定，我们将 BL 与链接返回地址到 `$r1` 的 JIRL 识别为调用指令，将 `JIRL $r0, $r1, 0` 识别为返回指令。RAS 维护推测与架构两份拷贝：推测拷贝随预测消费压栈/弹栈，架构拷贝只在提交时更新；发生误预测时以架构拷贝整体重建推测拷贝，从而精确撤销错误路径上调用指令对栈内容的覆盖。

在预测策略上，静态目标跳转（B/BL）不经 PHT、恒预测跳转；JIRL 的方向仍由 PHT 门控；返回指令的目标取自 RAS 栈顶，其余跳转目标取自 BTB。每条被预测的指令都携带一份预测恢复信息（查询时的 GHR 快照与包内前序分支数），随指令行至提交点；提交时分支预测器按照真实结果精确重建 GHR 与 RAS，并对 PHT 中的当前计数器做饱和更新，实现预测状态的修复。

=== AI 分支预测器

常规预测器扩容后，数据相关分支仍占据较多误预测。经典研究早已证明，感知器 AI 网络能够学习远超双位计数器所能表达的分支相关模式：Jiménez 与 Lin 首次将感知器用于分支方向预测 @perceptron-hpca2001，其随后的路径感知器方案 @perceptron-micro2003 与 Seznec 的 TAGE-SC-L @tage 代表了「以更长历史换取更高精度」的两条主流路线；以 BranchNet 等为代表的新近研究进一步将卷积神经网络引入分支预测，专门面向「系统性难预测」分支 @branchnet（相关综述见 @dl-bp-survey）。工业界同样验证了这一思路：AMD Zen 2 的二级方向预测器最初即采用感知器结构，后迁移至 TAGE @zen2；三星自研 Exynos CPU 的演进论文也描述了基于感知器的分支预测 @exynos。开源高性能核的实践则给出了接入方式的答案：BOOM 将快速 next-line 预测器与较慢的 backing predictor 分离，慢预测的晚到高置信结果才覆盖快预测 @boom；香山昆明湖以单拍 uFTB 提供无气泡预测，再以 FTB、TAGE-SC、ITTAGE 等较慢表在后续拍覆盖 @xiangshan。

受相关工作的启发，Gemmont 引入 AI 进行分支预测，在快速路径之后加入四级流水的残差感知机。模型根据更长的分支历史判断是否翻转快速预测，并在重命名前给出晚修正；结果未及时返回时直接沿用快速预测，因此不会拉长取指关键路径。权重离线训练并量化为 4 位整数，推理过程只包含定点加法。仿真中，神经网络修正器把整核提交分支误预测从 22.0 万次降至 10.8 万次（约 −51%），模型给出的修正中约 94% 对最终预测有益。

#figure(
  table(
    columns: (1.2fr, 1fr, 1fr, 0.8fr),
    inset: 0.5em,
    align: (x, y) => if x == 0 { left + horizon } else { center + horizon },
    table.header([*指标*], [*仅快速预测*], [*加入晚修正*], [*变化*]),
    [分支误预测率], [3.3714%], [1.6802%], [-50.16%],
    [总周期], [28,545,944], [27,439,712], [-3.88%],
    [相对性能], [1.000], [1.040], [+4.032%],
  ),
  caption: [AI 分支预测器消融结果（20 个性能样例）],
) <tab:ai-bp-ablation>

=== 重命名与分发

*重命名阶段。*Gemmont 以显式重命名消除 WAR 与 WAW 相关。我们维护的物理寄存器堆（PRF）中共有 63 项（另有硬连线恒零的 p0），对于每个逻辑寄存器（去除 `$r0` 共 31 个），用重命名映射表（RAT）记录其当前映射的物理寄存器索引。RAT 维护两个实例：推测版本 sRAT 永远存储乱序流水线最新的映射关系，架构版本 aRAT 只在指令提交时更新，供流水线恢复时回滚使用。

物理寄存器的分配与回收由 32 项空闲寄存器列表（Free List）完成：复位时 r1\~r31 映射到 p1\~p31，自由列表填充 p32\~p63；列表预取 3 项候选，使分配可以在本阶段组合完成。对于同组内的 3 条指令，重命名逻辑对每条更早的指令做内容寻址比较，将源操作数前递到同组内最新写入的新物理寄存器，从而同时解决同组的 RAW 与 WAW 相关。

*分发阶段。*重命名握手原子地完成 ROB 分配、sRAT 更新与自由列表弹出，并将同一个 3 指令包锁存至分发寄存器；随后只有在该包所需的全部发射队列入口均就绪时，才整体完成分发。因此同一指令包不会只进入部分目标队列。分发逻辑按照译码阶段产生的功能单元类型将指令分类并压缩，装入三个类型化发射队列：整数发射队列（7 项，乱序，3 发射）、乘除发射队列（3 项，顺序）与访存发射队列（5 项，顺序）。带有前端已发现异常的微操作不进入任何发射队列，其例外随 ROB 至提交点处理；不需要任何功能单元执行的指令（如 SYSCALL、ERTN、TLBSRCH、TLBRD、TLBWR 与 TLBFILL）同样不入队，直接在 ROB 中等待提交；INVTLB 则进入整数发射队列取得操作数。

== 后端

#figure(
  image("figs/backend.drawio.pdf", width: 100%),
  caption: [Gemmont 后端结构],
) <fig:backend>

后端包括 7 项整数发射队列、3 项乘除发射队列和 5 项访存发射队列。整数队列按年龄乱序选择，其余两类队列顺序发射。操作数状态由物理寄存器的忙表、写回旁路和局部唤醒共同维护，相关指令不必等待结果写回寄存器堆后再启动。

5 条执行流水线包括 3 条整数流水线、1 条乘除与点积流水线，以及 1 条访存流水线。整数流水线承担算术、逻辑、分支和特权操作；乘法采用流水实现，除法采用迭代实现。访存流水线的地址生成、缓存访问和存储排序单独在下一节说明。

执行结果写入物理寄存器并更新 32 项 ROB。ROB 每拍最多顺序提交 3 条指令，寄存器映射回收、CSR 写入、TLB 操作和存储提交均以此为界。异常或分支误预测发生时，后端清除年轻指令并恢复重命名状态，从而保持精确异常和顺序的架构可见结果。

=== 指令发射与唤醒

*发射阶段。*这一阶段判断发射队列中哪些指令可以被发射。整数发射队列保持压缩存储，槽位下标越小指令越老，选择逻辑按最老优先的优先级编码为三条发射通道顺序授权；三条通道功能受限——通道 0 仅执行 ALU 操作，通道 1 附加定时器读取，通道 2 附加比较/CSR/INVTLB 操作，分支、CSR 与 INVTLB 指令固定由通道 2 执行。被发射的槽位由更年轻的表项下移填平，维持年龄序不变式。乘除发射队列（自定义 `DP4` 指令与其共享）与访存发射队列各只有一个执行单元，故采用顺序 FIFO，仅发射队头。

操作数就绪性在分发时由物理寄存器堆的 busy 表给出初值，入队后由三条唤醒路径维护：
1. 五路全局写回唤醒，对所有在队操作数做内容寻址比较；
2. 整数流水线的本地唤醒，本拍被选中发射的生产者乐观地唤醒其下游操作数；
3. 跨时序寄存器的同拍唤醒，使寄存化后的访存唤醒仍可参与当前拍的选择。对于 load 指令，我们采用推测唤醒：访存流水线在发起缓存请求的同一拍即假设命中并唤醒依赖指令；若随后发现缓存缺失或流水停滞，失败信号经寄存后冻结整数与乘除流水线的读寄存器级，直至该 load 真正就绪。被错误唤醒且已经发射的依赖指令保持在读寄存器级，期间不继续执行。

=== 整数执行流水线

整数执行流水线共三条，均为 ISS、RRD、EXE、WB 四段固定延迟，共享同一结构的组合执行数据通路，包含 ALU、移位器、比较器、分支解析、CSR 读取与 TLB 操作数打包等逻辑。

为了缓解算术指令的 RAW 相关，流水线在读寄存器级即提前清除目的寄存器的 busy 位：生产者在 RRD 拍「预告完成」，消费者于下一拍发射，待其进入执行级时，生产者恰好处于写回拍，其结果被五条写回旁路之一前递为执行级的输入。这样，具有 RAW 相关的指令对可以背靠背地每拍执行一条。

分支指令在执行级解析方向与目标。通道 2 的实际目标信号被三重复用：对普通分支是重定向目标，对 CSR 写指令是 CSR 写数据，对 INVTLB 指令是打包后的操作数；该信号经 ROB 送达提交点统一消费。CSR 读地址由通道 2 的执行级组合驱动，CSR 写操作则推迟到提交点生效。

=== 乘除执行流水线

乘除执行流水线的执行级包含一个流水化的 32 位乘法器与一个恢复余数迭代除法器。乘法器稳态每拍可接受一个新请求；除法器每拍产生 1 位商，当有符号或无符号操作数的幅值都可由 16 位表示时改用 16 位除法器，迭代拍数由 32 拍减半为 16 拍。除法请求仅在乘法流水线和响应端均为空时接收，因此不会与在途乘法争用响应；两者共享单一写回口。完成信号在乘法末级或除法末次迭代时提前一拍唤醒目的寄存器，下一拍经共享响应口写回。

=== DP4 点积扩展 <sec:dp4>

为加速板上大语言模型推理，我们引入自定义的 `DP4` 指令扩展。`DP4 rd, rj, rk` 将两个源寄存器各自拆成 4 个有符号 8 位数，完成四路乘加并写回 32 位结果。这一运算对应 W8A8 量化矩阵乘的内积，避免用多条移位、乘法和加法指令展开。DP4 与乘除单元共用发射队列和写回端口，同时使用独立的组合乘加树。

#figure(
  table(
    columns: (1.3fr, 1fr, 1fr, 0.8fr),
    inset: 0.5em,
    align: (x, y) => if x == 0 { left + horizon } else { center + horizon },
    table.header([*指标*], [*INT32 Cast*], [*DP4*], [*加速比*]),
    [Prefill], [0.130 tok/s], [0.205 tok/s], [1.58x],
    [Decode], [0.107 tok/s], [0.165 tok/s], [1.54x],
    [端到端生成 8 个 token], [127.3 s], [81.6 s], [1.56x],
  ),
  caption: [DP4 在 80M 参数模型上的板上推理加速],
) <tab:dp4-performance>

=== 访存执行流水线

访存执行流水线为七级结构：ISS、RRD、ADDR、MEM1、MEM2、WB、WB2。流水线携带两条相互独立的载荷流：其一为发射队列来的投机访存微操作，其二为 Store Buffer 弹出的已退休 store 或重放的未缓存 load；后者可占用空闲的发射槽，或与一条更年轻的 store 的地址计算共发射。

ADDR 段完成地址生成、字节使能与写数据对齐、虚实地址翻译以及对齐/翻译异常的生成。LL/SC 指令在此快照架构 LLBit；失败的 SC 不发起任何访存、也不产生异常，仅向目的寄存器写 0。MEM1 段发起数据缓存请求，并执行前述的推测唤醒。load 的每个结果字节会按覆盖优先级，从数据缓存响应、RRD 至 WB 各级在途的已退休 store、Store Buffer 查询结果，以及尚待推入 Store Buffer 的同拍 store 中选择；同地址上更年轻的 store 数据覆盖更老来源。整条流水线是非弹性的：MEM2 段一旦停滞，反压将一路传递至发射级，使依赖指令无法越过缺失的 load 采样到旧的物理寄存器值。

无异常且实际生效的 store，以及未缓存 load，在 MEM2 段推入 Store Buffer。Store Buffer 的逻辑总容量为 8 项；内部虽另设 2 项已退休排出寄存器，但主队列与排出寄存器的总占用仍限制为 8。store 仅在提交点被标记退休后，才经排出寄存器以 STD 载荷重新进入流水线：缓存 store 写入数据缓存，未缓存 store 进入未缓存总线通路；这保证存储状态只被已提交的指令改变。未缓存访问先探测数据缓存中的脏别名（必要时写回并失效），再经核内未缓存 AXI 通路发起事务；未缓存 load 采用两阶段提交——先在 ROB 头部由提交状态机发起总线读（期间屏蔽中断），再以 STD 载荷重放流水线取回数据并写回。在冲刷恢复时，投机载荷全部被杀死，但已被缓存或总线接受的 STD 事务将被保留直至排出完毕，保证已退休的 store 不会丢失。

=== 指令写回与提交

*写回阶段。*执行结果写入物理寄存器堆（并带同拍旁路），目的寄存器的 busy 位被清除，指令的最终执行状态经五个完成端口更新到 ROB 中，等待顺序提交。ROB 共 32 项，静态信息与动态状态分离存储；前端已发现异常与无需执行的指令在分配时即为完成态，非控制转移指令却被预测为跳转的在分配时即为误预测态（该标记按或积累，不会被后续完成写覆盖）。提交侧每拍预取 3 个表头项，并叠加同拍的分配与完成更新。

*提交阶段。*精确异常要求指令顺序提交。ROB 的就绪顺序前缀受五项条件约束：前缀全部完成、前缀内无异常或线性恢复事件、串行化指令单独提交、前缀内无误预测事件，以及未缓存 load 的两阶段提交状态机。三条提交通道均可提交普通 GPR 写回并更新 aRAT、回收旧物理寄存器；CSR 写、TLB 操作、缓存维护（CACOP）、ERTN、IDLE 进入、LLBit 更新、store 提交以及分支预测器更新等全局副作用则集中在 0 号端口生效。

流水线的状态恢复采用两级协议：第一级在提交拍当拍生效——重定向前端、清除 ROB、发射队列、执行级与访存流水线的全部推测状态；第二级在下一拍恢复重命名表与自由列表。后端各结构对两条边沿均保持静默，避免恢复窗口内年轻的访存操作多推进一级。对于改变机器全局状态的指令（CSR 写、TLB 操作、CACOP、ERTN、LL/SC 等），提交时冲刷整条流水线：除 ERTN 外的线性恢复通常从 `pc+4` 重新取指，ERTN 则由特权控制器重定向至 ERA，使后继指令在新的全局状态下重新流过前端，实现串行化与精确异常。其中，例外进入、ERTN 返回以及写 CRMD/DMW0/DMW1 等改变取指快速映射语义的提交，其重定向目标被保留一拍、待取指侧按提交后的 PLV/DA/PG/DMW 状态完成翻译后再启动，避免重定向使用过期的直接映射或直接映射窗口状态；分支、ASID 与 TLB 等其余恢复仍走即时路径。

== 访存子系统

#figure(
  image("figs/ls.drawio.pdf", width: 100%),
  caption: [Gemmont 缓存层级与访存通路],
) <fig:memory-system>

=== 缓存层级与存储顺序

一级指令缓存和一级数据缓存均为 8 KiB、两路组相联，统一二级缓存为 64 KiB、两路组相联，三者的缓存行均为 64 B。统一 L2 同时接收取指、数据和写回请求。在板上实测中，一次核外访存约需 180 周期，L2 可截获一级缓存缺失，减少核外访问次数。

访存流水线完成地址生成、TLB 查询、对齐检查和一级数据缓存访问。8 项 Store Buffer 暂存投机存储，store 只有在 ROB 提交后才写入缓存或总线；load 可从更老的 store 转发数据。未缓存访问和 LL/SC 也在提交边界上排序，流水线冲刷不会丢失已经提交的存储。

=== 推测唤醒与数据预取

为缩短 load 到使用者的相关延迟，数据缓存请求发出时会先按命中路径唤醒依赖指令；若随后发现缺失，相关流水级暂停，待数据返回后继续。

一级数据缓存还设置了顺序流预取器。它跟踪连续缓存行访问，只预取已经命中 L2 的数据；预取行先保存在独立缓冲中，需求访问命中后再装入一级缓存。

#figure(
  table(
    columns: (1.2fr, 1fr, 1fr, 0.8fr),
    inset: 0.5em,
    align: (x, y) => if x == 0 { left + horizon } else { center + horizon },
    table.header([*优化*], [*启用前总周期*], [*启用后总周期*], [*性能增益*]),
    [统一 L2 缓存], [56,854,185], [27,439,712], [+57.09%],
    [load 推测唤醒], [31,335,743], [27,439,712], [+14.20%],
    [L1 数据预取], [28,427,988], [27,439,712], [+3.602%],
  ),
  caption: [访存相关优化的消融结果（20 个性能样例）],
) <tab:memory-ablation>

== 外部接口

Gemmont 核的外部接口按照 Chiplab SoC 规定的处理器核外部接口设计。具体来说，Gemmont 核通过 AXI3 协议与外部进行数据通信，总线位宽 32 位，并接收外部传入的 8 位硬件中断。在核内部，一级指令缓存与一级数据缓存分别通过行请求/响应协议接入统一 L2；未缓存 AXI 通路先经过深度为 1024 的写缓冲，再接入 L2 的未缓存端口。L2 统一调度这些请求，并经顶层适配器只暴露一个对外 AXI 接口。此外，Gemmont 核提供面向 Chiplab Difftest 的架构观测接口，观测信号不参与任何数据通路控制。

= SoC 设计 <sec:soc>

为展示 Gemmont 处理器的功能，我们在大赛 Chiplab 平台 `loongson` 系统演示 SoC 的基础上扩展了板载外设支持。除 Gemmont 核外，SoC 中可驱动的设备列举如下。

- DDR3 DRAM，用作主存（128 MiB）
- NAND Flash，用作外存（128 MiB）
- SPI Flash，用作固件，烧录启动引导程序
- UART 串口控制器
- 以太网控制器（DMFE MAC）与 APB DMA
- GPIO（confreg），驱动单色 LED、双色 LED、七段数码管、拨码开关与按键
- VGA 输出设备
- NT35510 LCD 显示器
- PS/2 键盘
- USB 主控制器（支持热插拔）

SoC 的结构如 @fig:soc 所示。Gemmont 核经 AXI 时钟转换后接入 33 MHz 的核外时钟域，由 AXI 从机多路复用开关分发到七个从机端口；DDR3 一侧的 AXI 互联扩展为四个从机口，除处理器外还接纳以太网 DMA、APB DMA 与 VGA 帧缓冲读取三个主口的访存流量。

#figure(
  image("figs/soc.drawio.pdf"),
  caption: [SoC 结构图],
) <fig:soc>

== 地址分配与中断分配

SoC 对各外设的地址映射规则如 @tab:addrmap 所示。其中 PS/2 与 NT35510 控制器挂载于 AXI-APB 桥之下，复用 APB 地址空间的高 4 位进行译码。

#figure(
  table(
    columns: (1fr, auto, auto, auto),
    inset: 0.6em,
    table.header([*外设*], [*起始地址*], [*结束地址*], [*分配大小*]),
    [DDR3 内存], [`0x00000000`], [`0x07ffffff`], [128 MB],
    [SPI Flash], [`0x1c000000`], [`0x1c0fffff`], [1 MB],
    [VGA TFT 控制器], [`0x1d010000`], [`0x1d01ffff`], [64 KB],
    [PS/2 控制器], [`0x1d020000`], [`0x1d02ffff`], [64 KB],
    [NT35510 控制器], [`0x1d030000`], [`0x1d03ffff`], [64 KB],
    [USB 主控制器], [`0x1d040000`], [`0x1d040fff`], [4 KB],
    [confreg（GPIO）], [`0x1fd00000`], [`0x1fd0ffff`], [64 KB],
    [UART0], [`0x1fe001e0`], [—], [APB 外设区],
    [NAND 控制器], [`0x1fe78000`], [—], [APB 外设区],
    [以太网 MAC], [`0x1ff00000`], [`0x1ff0ffff`], [64 KB],
  ),
  caption: [SoC 地址映射表],
) <tab:addrmap>

LA32R 指令架构规定了 8 个硬件中断位。SoC 选用其中的低 7 位，最高位置零，具体分配如 @tab:hwintr 所示。

#figure(
  table(
    columns: (1fr, auto, auto),
    inset: 0.6em,
    table.header([*外设*], [*硬件中断位*], [*ESTAT IS 位*]),
    [以太网 MAC], [`HWI0`], [`IS[2]`],
    [UART0], [`HWI1`], [`IS[3]`],
    [SPI Flash], [`HWI2`], [`IS[4]`],
    [NAND], [`HWI3`], [`IS[5]`],
    [APB DMA], [`HWI4`], [`IS[6]`],
    [PS/2], [`HWI5`], [`IS[7]`],
    [USB 主控制器], [`HWI6`], [`IS[8]`],
  ),
  caption: [SoC 硬件中断分配表],
) <tab:hwintr>

== 外设控制器

考虑到控制器实现与软件驱动设计的复杂性，SoC 优先复用经过验证的已有控制器，仅对没有合适现成方案的外设自行设计，详见 @tab:ctrl。

#figure(
  table(
    columns: (auto, auto, 1fr),
    inset: 0.6em,
    table.header([*外设*], [*控制器来源*], [*说明*]),
    [DDR3], [Xilinx MIG（Chiplab）], [128 MiB 主存],
    [SPI Flash], [Chiplab], [固件存储，烧录 U-Boot],
    [UART], [Chiplab], [NS16550 兼容，115200 波特],
    [NAND Flash], [Chiplab], [128 MiB 外存],
    [APB DMA], [Chiplab], [存储 DMA],
    [以太网], [Chiplab], [DMFE，Davicom DM9102 编程模型],
    [GPIO], [Chiplab confreg], [LED、数码管、拨码开关、按键],
    [VGA], [Xilinx AXI TFT], [帧缓冲位于 DDR3],
    [NT35510 LCD], [自行设计], [寄存器布局参考 NOP-SoC @nop],
    [PS/2], [自行设计], [Altera UP 兼容寄存器接口],
    [USB host], [UE11 开源核 @usb-host + 自行封装], [USB 1.1 Full-Speed，支持热插拔],
  ),
  caption: [SoC 控制器配置],
) <tab:ctrl>

= 系统软件

在功能测试与性能测试的基础上，我们使用 U-Boot 引导程序与 Linux 操作系统验证 Gemmont 实现的正确性，并搭建了完整的用户态软件栈以支撑系统演示。

== U-Boot 与启动流程

我们使用龙芯教育移植的 LA32R U-Boot @la32r-uboot 作为启动引导程序，采用其发布版本的预编译镜像。烧录流程已自动化：先向 FPGA 下载串口在线编程 flash 的简易 SoC 比特流，再经 USB-UART 以 XMODEM 协议将 U-Boot 写入板载可插拔 SPI Flash。

上电复位后，处理器从 SPI Flash 执行 U-Boot；随后经以太网以 TFTP 协议加载 Linux 内核镜像，`bootelf` 命令按 ELF 段重定位后跳转至内核入口，U-Boot 的 `bootcmd` 环境变量作为内核启动参数字符串传入。大赛提供的 Linux 5.14.0-rc2 内核与我们自行移植的 Linux 7.1.4 主线内核均可经此流程稳定启动。

== Linux 7.1.4 移植

原有 LA32R 内核停留在 2022 年的 5.14.0-rc2。我们审计了 la32r-Linux @la32r-linux 此后的 95 个提交，并将相关改动重新适配到 Linux 7.1.4。主要工作包括处理器探测、异常与 TLB 重填、缓存和非一致 DMA、精简指令集兼容以及系统调用 ABI。

== 板级支持与设备驱动

板级支持方面，以内置设备树 `loongson-chiplab.dts` 与专用配置 `chiplab_la32r_``defconfig` 提供。设备树描述了第 @sec:soc 章中的全部外设节点（串口、confreg、VGA、PS/2、LCD、USB、以太网、APB DMA、NAND）及其地址与中断，并固化启动参数；由于 LA32R 没有恒定频率发现机制，处理器频率经 `cpuclock` 启动参数注入。内核启动参数由 `CONFIG_CMDLINE_FORCE` 强制生效，其中 NFS root 相关参数为 `root=/dev/nfs rw nfsroot=<服务器>:<导出路径>,vers=3,tcp ip=<静态 IP 配置>`，使得引导程序传入的参数不再影响系统挂载方式。

设备驱动方面，我们实现了如下驱动：

- *confreg。* 新写平台驱动，向三个子系统注册：LED class（16 个单色 LED 与两个双色 LED 的各通道，出现在 `/sys/class/leds/`）、GPIO 控制器（8 路拨码开关输入）以及整寄存器读写的 sysfs 属性。
- *NAND 与 APB DMA。* 复用主线 Loongson-1 系列驱动并加以安全加固：提供只读探测模式（绝不写、绝不擦）、全路径有界超时与控制器恢复、中断加轮询的双完成机制（Chiplab 的 DMA 中断为短脉冲，跨时钟域可能丢失）、DMA 终止失败时保留内存映射并永久禁用控制器。存储栈规定 `kernel` 分区只读，可写文件系统必须使用 UBI/UBIFS。
- *以太网。* 新写 DMFE 的 platform 驱动。关键修复为：写 CSR6 停止发送后，轮询收发状态机真正进入停止态再写入启动值——该 MAC 仅在两个状态机均停止时才锁存启动请求，FPGA 主频下固定延时不足会导致发送永久停摆。
- *VGA。* 板级 VGA 输出即 Xilinx XPS TFT 控制器，直接复用主线 `xilinxfb` 帧缓冲驱动；帧缓冲由 CMA 一致性内存分配，注册为 `fb0` 并承担帧缓冲控制台。
- *LCD。* 新写 `chiplab-nt35510fb` 帧缓冲驱动：帧缓冲为系统内存，经 deferred I/O 周期性地将脏页矩形通过 8080 总线推入面板显存，注册为 `fb1`；初始化序列源自清华开源实现 @nop。
- *PS/2。* 设备树绑定 Altera PS/2 兼容串口，经 serio 子系统、标准 AT 键盘翻译驱动接入输入子系统，事件由 evdev 导出。
- *USB。* 新写专用主控制器驱动（HCD，约 1700 行，代码骨架参考 SL811HS HCD）：实现虚拟根集线器，探测时校验硬件版本寄存器；_热插拔_经根集线器状态轮询实现：每次轮询采样线路状态、置位连接变化位，断开后经多次采样去抖。配合 USB storage 与 HID 类驱动，U 盘与 USB 键鼠均可即插即用。

== 根文件系统与系统应用

根文件系统由 Buildroot 2026.05.1 @buildroot 构建。我们加入 LA32R/ILP32S 外部工具链支持，从源码构建 GCC 16.1.0、Binutils 2.46.1 与 glibc 2.44，并修正 glibc 中误用 LA64 指令的兼容问题。交叉工具链用于构建系统，板上原生 GCC 则可直接编译和运行 C 程序。

我们完成了 Go 语言工具链向 LA32R 的移植（基于 Go 1.26.5 新增 `loong32r` 架构，含汇编器、编译器与运行时），并以此交叉编译运行了 PicoClaw 智能体 @picoclaw。配合 @sec:dp4 中的自定义 `DP4` 点积指令，我们还在板上运行了自研的 Gemmont-LM 小模型推理框架，打通了「自定义 AI 指令 → 内核 → 用户态推理框架 → Agent」的软硬件协同完整 AI 推理链路。

#figure(
  image("figs/picoclaw.png", width: 70%),
  caption: [PicoClaw 在板上检查环境、编写并运行 C 程序],
) <fig:picoclaw>

#figure(
  table(
    columns: (auto, 1.8fr, 1.6fr),
    inset: 0.5em,
    align: (left + horizon, left + horizon, left + horizon),
    table.header([*功能类别*], [*主要软件*], [*作用*]),
    [构建与运行环境],
    [Buildroot 2026.05.1、BusyBox、glibc 2.44、NFS root、UBI / UBIFS],
    [构建根文件系统，提供基础命令与 C 运行时，支持网络启动和持久化存储],

    [C/C++ 开发工具链], [GCC 16.1.0、Binutils 2.46.1], [交叉编译系统软件，并支持在板上原生编译 C 程序],
    [命令行与开发环境],
    [Bash 5.2.37、Vim 9.1、tmux、htop、fastfetch、tree],
    [Shell 操作、文本编辑、终端会话管理、进程监控和系统信息查看],

    [远程访问与设备管理],
    [OpenSSH、lrzsz、adb / adbd、iproute2、ethtool、NTP / DHCP 客户端、OpenSSL 与 CA 证书],
    [SSH / SCP / SFTP 登录、串口传输、网络配置、时间同步和安全通信],

    [Web 与 Python 应用],
    [nginx 1.30.4、Python 3.14.7、libffi 3.7.1、Django、requests],
    [提供 HTTP / HTTPS / HTTP2 服务，运行 Python 程序和 Web 应用],

    [Go 语言生态], [Go 1.26.5 汇编器、编译器与运行时], [为 LA32R 构建和运行 Go 程序],
    [图形桌面],
    [X.Org Server 21.1.23、Fluxbox 1.3.7、NODM、Xterm、Xcalc、Xedit、Xclock],
    [在 VGA 上启动图形桌面和常用 X 应用],

    [输入与显示调试], [evdev、evtest、fb-test、fbdump、fbv], [接入 PS/2 和 USB 输入设备，测试和查看 VGA / LCD 帧缓冲],
    [性能评测], [CoreMark 1.01、Dhrystone 2.1], [在板上运行处理器性能基准测试],
    [AI 应用], [Gemmont-LM、PicoClaw], [运行 W8A8 模型推理和 Go 实现的智能体应用],
  ),
  caption: [Gemmont 的用户态软件栈],
) <tab:software-stack>

#figure(
  image("figs/vga.jpg", width: 60%),
  caption: [使用 VGA 终端展示 `fastfetch`],
)

#figure(
  image("figs/lcd.jpg", width: 60%),
  caption: [使用 LCD 展示特定图片],
)

#figure(
  image("figs/x_usb.jpg", width: 60%),
  caption: [使用 USB 键盘操作 X 桌面],
)

= 相关工作

== LA32R 单发射处理器

openLA500 @openla500 是大赛官方提供的 LA32R 基线处理器，也是性能评分的基准：各测试点以相对其周期数的加速比计分。它采用单发射五级流水，具备异常处理与虚实地址翻译功能，并配有简单的分支预测与指令/数据缓存等加速手段，可以启动 Linux 系统。openLA500 能达到较高的主频，但没有对超标量执行与动态调度做出探索，IPC 存在不足。

== 「龙芯杯」乱序处理器

NOP @nop 是 NSCSCC 2023（第七届「龙芯杯」）特等奖作品，由清华大学团队开发，也是首个开源的 LA32R 乱序多发射处理器。NOP 采用基于 Tomasulo 动态调度算法与重排序缓存的乱序架构，最长 13 级流水，每周期取指至多 4 条，译码、重命名、分发、提交均为 3 宽；配置 32 项 ROB 与 63 项物理寄存器堆，后端包含三条整数流水线与乘除、访存流水线各一路，配备 8 项 Store Buffer，并采用地址翻译拆级与 load 推测唤醒等优化手段。NOP 在大赛 FPGA 平台上达到 107.69 MHz 主频与 1.02 的绝对 IPC，相对 openLA500 基线的总体加速比为 3.00，成为后续 LA32R 乱序处理器开发的重要基线。本项目的整体乱序微架构设计在初期阶段参考了 NOP 的开源实现，谨致谢意。

GeekPie\_Core @geekpie 是 NSCSCC 2025（第九届「龙芯杯」）特等奖作品，由上海科技大学 GeekPie\_ 团队开发。其前端每周期取指 4 条并配置 16 项指令缓冲，译码、重命名、提交均为 3 宽；配置 32 项 ROB 与 64 项物理寄存器堆，后端共四组执行单元。访存子系统是其主要亮点：store 地址与数据通路分离，load/store 队列各 8 项，支持部分乱序访存；数据缓存采用非阻塞结构并配置 4 项 MSHR，在 8 KiB 一级指令/数据缓存之外另设 64 KiB 二级缓存，缓存行 64 字节，采用 256 位接口，命中延迟约 5 拍。GeekPie 在大赛 FPGA 平台上达到 107.69 MHz 主频，周期数相对 openLA500 的加速比几何均值达 1.98，总体加速比达 6.52，代表了 2025 年「龙芯杯」乱序处理器的最高水平。本项目的二级缓存设计参考了 GeekPie\_Core 的开源实现，谨致谢意。

= 参考文献

#bibliography("ref/refs.yml", title: none, style: "ref/gbt7714-numeric.csl")
